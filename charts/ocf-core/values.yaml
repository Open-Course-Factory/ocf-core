service:
  type: ClusterIP
  port: 8080

image:
  repository: registry.gitlab.com/ci-cd-chihi-mecheri-michel-gueye/ocf-core
  tag: v1

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: ocf.local
      paths:
        - path: /
          pathType: Prefix
